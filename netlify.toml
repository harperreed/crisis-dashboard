# ABOUTME: Netlify deployment configuration
# ABOUTME: Specifies build settings and publish directory

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
